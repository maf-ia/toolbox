from PIL import Image 


img = Image.open( "spiral-1.gif")
#img = img.convert('RGB')
pix = img.load()
w,h = img.size

#backColor = imgPix[0, 0]

from PIL import ImageDraw
i = Image.new("RGB", (5000,1000), "#444")
draw  =  ImageDraw.Draw(i) 
draw.line( ( (offX,offY), (newOffX,newOffY) ) )
i.save( "sawed.png" )